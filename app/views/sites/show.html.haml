= render partial: "shared/page_header", locals: { title: "#{@site.name}(#{@site.tracks_count})", link: nil }

%span.text-hint Number of tracks by each district
.mb-2

%ul
- @tracks.each do |track|
  %li= track.code
