.container-fluid
  .row
    .col-lg-12
      = render "shared/sb-admin/page_heading", title: @site.name
      %span.text-hint
        = t(".title")
    .col-lg-4
      %table.table.mt-4
        %tbody
          - @tracks.each do |track|
            %tr
              %td= track.code
