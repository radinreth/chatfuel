.d-sm-flex.align-items-center.justify-content-between.mb-4
  %h1.h3.mb-0.text-gray-800= t(".title")
  = link_to new_template_path, class: "btn btn-primary mb-3" do
    %i.fa.fa-fw.fa-plus
    = t(".new")

%h6.text-dark
  = t(".incomplete")
.row.card-deck.mb-5
  = render partial: "template", collection: @templates.incomplete

%h6.text-dark
  = t(".complete")
.row.card-deck.mb-5
  = render partial: "template", collection: @templates.completed

%h6.text-dark
  = t(".invalid")
.row.card-deck.mb-5
  = render partial: "template", collection: @templates.incorrect