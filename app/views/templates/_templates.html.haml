- if @templates.send(collection).count > 0
  %h5.text-header
    = t(".#{collection.to_s}")
  .row.card-deck.mb-5{ id: collection }
    = render partial: "template", collection: @templates.send(collection)
