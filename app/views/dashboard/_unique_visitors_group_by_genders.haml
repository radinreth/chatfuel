- Variable.gender.values.each do |gender|
  - if gender.plain_values.include?(session.gender)
    %li.list-inline-item
      %small.text-gray-800
        = "#{gender.mapping_value}: #{session.gender_count}"
    - break
