= simple_form_for @user do |f|
  = f.input :role, as: :radio_buttons, collection: User.roles, value_method: :first
  = f.input :email
  = f.input :password
  = f.input :password_confirmation
  = f.submit
