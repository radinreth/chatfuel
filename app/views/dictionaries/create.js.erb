<% if @variable.errors.any? %>
  $("#new_variable").html("<%= j(render 'form', dictionary: @variable) %>");
<% else %>
  $("#dataTable tbody").prepend("<%= j(render 'dictionary', dictionary: @variable) %>")
  $("[data-dismiss]").click();
<% end %>
